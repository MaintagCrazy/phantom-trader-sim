[phases.setup]
nixPkgs = ["nodejs_20", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build",
  "npx prisma db push --accept-data-loss || echo 'DB push skipped - will retry at runtime'"
]

[start]
cmd = "node dist/index.js"
